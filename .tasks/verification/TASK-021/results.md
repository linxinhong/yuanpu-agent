# App 多入口与升级完整业务验收（TASK-021）

## Target and independent scenario matrix

- Verifier: independent of TASK-019/020 implementation. Initial matrix fixed before reading their result reports.
- Source: TASK-021 card; `docs/agent-runtime-contracts.md` (run, delivery, migration and lifecycle); `docs/im-channel-contract.md` (IM receipt/offline boundary); `docs/frontend/tasks/connection-schedule-management.md` (approved App navigation); `docs/adr/0003-defer-native-notifications.md`.
- Initial revision: `31fe1a3859821366bf12b3cc83dd74b1fcad4dac`, clean `task/task-021-business-acceptance`; macOS arm64 host. The matrix below was written before execution; outcomes follow in the next section.
- Retrieval: ZG semantic query for TASK-021 cross-component App/IM/schedule/restart/SEA journey returned fresh hits in the card, UI contract and run contract; focused `rg` followed. The referenced `docs/application-architecture.md` is absent from this worktree (user-owned untracked main document); the tracked card/contract/UI decision are used here.

| ID | Actor/start and operation | Expected authoritative result | Mode needed | Status |
| --- | --- | --- | --- | --- |
| V21-01 | App owner configures a connection with an isolated credential, exchanges an authorized private IM message, creates a bound scheduled task, opens task/run/IM delivery and returns to its conversation | Authenticated connection; one authorized run and reply; persisted schedule/trigger/run/outbound identities agree; App labels run and delivery separately, retains navigation after refresh | Real App + authorized provider for full claim; fixture for isolated fallback | unverified |
| V21-02 | App owner exits during an enabled future schedule, restarts after due time | No App background service after quit; persisted schedule and misfire policy determine one recorded trigger/skip, without duplicate side effects; UI reflects actual history | Isolated App/Runtime + real SQLite | unverified |
| V21-03 | App owner exits while a run executes, then restarts | Active run recovers as `interrupted` if no possible external effect, otherwise `result_unknown`; no silent replay; App can inspect it | Isolated App/Runtime + controlled model | unverified |
| V21-04 | App owner exits while an IM result is `delivering`, then restarts | Persisted outbound is `unknown` if write/receipt uncertain; Agent is not rerun or blindly resent; App distinguishes delivery uncertainty from run success | Isolated App/Runtime + controlled provider | unverified |
| V21-05 | App owner stages a genuine SEA update, quits and launches App | Valid binary activates on next launch; health/stability confirmation; existing schedule, run, binding and conversation metadata remain usable | Packaged/current-platform SEA + isolated data | unverified |
| V21-06 | App owner encounters failed provisional activation or incompatible protocol | Pointer and closed SQLite snapshot roll back on provisional failure; old data remains usable. Incompatible protocol has explicit user-facing error | Packaged/current-platform SEA + controlled faulty/incompatible candidate | unverified |
| V21-07 | Release/platform reviewer inspects macOS, Windows, Linux artifacts and trust | Executable evidence is platform-specific; absent target runs remain unverified; production signing/trust deficit is retained; native notification is excluded under TASK-029 | Each target OS/package | unverified |
| V21-08 | Independent reviewer audits all claims and required gate | Every claimed journey names revision, OS, real/fixture mode and evidence; `pnpm check` passes on tested tree; partial fixture results are not promoted to full real journey | Evidence audit + Node 24 workspace gate | unverified |

`accepted` provider receipts alone establish SDK acceptance, not human display. The IM contract does not promise offline message replay; any real provider result must report observed behavior without an invented expectation. Synthetic data and temporary `YUANPU_HOME`/Electron userData only; no real bot message or personal App mutation in this pass without coordination.

## Execution and verdicts (2026-09-23)

All commands below used source revision `31fe1a3859821366bf12b3cc83dd74b1fcad4dac`, macOS arm64, Node `24.15.0`, pnpm `11.22.0`, worktree-local build outputs and synthetic temporary homes. The only tracked-source change is this report, which is ignored by `.gitignore` until force-added. `pnpm` temporarily added an empty `pnpmfileChecksum` to `pnpm-lock.yaml`; that unrelated diff was removed after the gates. Private runner records live under the worktree git dir `coding-owner/` and do not contain credentials.

| ID | Expected versus observed / conclusion | Evidence and reproducible command |
| --- | --- | --- |
| V21-01 | **Unverified as a full real journey.** An isolated production Electron main/preload/Runtime and actual Vite renderer saved one disabled synthetic connection and one schedule; preview matched persisted `nextTriggerAt`, disabled state persisted, UI opened task and connection, Bot ID was not redisplayed, chat cancellation reached `cancelled`, and Runtime child exited. No authenticated real IM exchange, bound IM target, due schedule result navigation, or refresh/reopen of this complete path was exercised. Existing TASK-019 real private-chat and proactive-delivery evidence applies to earlier revisions and separate operations, not this composite journey. | `TASK_020_RENDERER_URL=http://127.0.0.1:5187/ python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-electron-management-retry --timeout 180 --require node -- node apps/desktop/test/task-020-management-app-probe.mjs`; record `1790162127802064000.json`, pass summary `schedulePersisted=true`, `connectionStatus=disabled`, `providerRequests=1`, `runtimeChildrenStopped=true`. First attempt `1790162116811541000.json` hit an immediate `.nav-item` lookup before renderer navigation mounted; identical rerun passed, so classify as probe timing flake, not demonstrated product defect. Vite was stopped after the probe. |
| V21-02 | **Unverified.** No App quit while a future plan was waiting, nor restart after its due time, was run on this revision. Scheduler fixture and SEA smoke exercise persistence but do not establish this App timing journey. | No executed scenario command. |
| V21-03 | **Pass, isolated App fixture.** With a controlled model request held open, production Electron IPC submitted one run; App quit removed Runtime child. Persisted SQLite and reopened IPC both reported the same `result_unknown` run with `externalEffectState=possible`; provider request count remained one; second quit also left no Runtime child. This proves the possible-effect branch, not the no-effect `interrupted` branch or a live IM run. | `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-electron-running-restart --timeout 180 --require node -- node apps/desktop/test/task-019-electron-app-probe.mjs`; `1790162090181654000.json`. The related Runtime recovery test initially exited before readiness when run before `pnpm check` built `dist` (`1790161851608263000.json`); after build the same test passed within `pnpm check`. |
| V21-04 | **Unverified as App quit/restart while IM send is in flight.** `scheduled-im-delivery.test.mjs` passed its authenticated HTTP/SQLite bind/create/revoke path and existing channel tests pass in the full gate, but no controlled in-flight provider send was coupled to an Electron exit in this pass. | Focused test in `1790161851608263000.json` passed 1/1; full gate below. |
| V21-05 | **Partial, so full scenario unverified.** Native `darwin-arm64` SEA built and smoke passed: real SEA reopened SQLite twice, executed scheduler persistence, exercised frozen Python MCP, and staged Runtime update smoke passed. Updater test staged a real Node executable and activated it on next startup with matching SHA-256. No packaged App was launched with existing task/conversation data and then updated through its own UI/lifecycle. | `pnpm build:native` record `1790161921708779000.json`; `pnpm smoke:native` record `1790161934803246000.json`; updater tests in `1790162158901222000.json`. All run through worktree-kit with `--require node --require pnpm` where applicable. |
| V21-06 | **Partial, so full scenario unverified.** Updater tests restored the old pointer and SQLite contents after a provisional migration/crash and rejected malformed staged artifacts. RuntimeManager test rejected a mismatched protocol with an explicit error and no restart loop (12/12 focused tests passed). This is controller/test-harness evidence; no packaged App user-facing incompatible-protocol screen or existing conversation was observed. | `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-protocol-and-update --timeout 180 --require node -- node --test apps/desktop/test/runtime-manager.test.mjs apps/desktop/test/runtime-updater.test.mjs`; `1790162158901222000.json`. |
| V21-07 | **macOS artifact built; release/platform scenario unverified.** `pnpm package:desktop` produced `YuanpuAgent-0.1.0-mac-arm64.{dmg,zip}` and an unpacked `.app` in this worktree. Packaging log says macOS application signing was skipped because no valid Developer ID identity was found; `codesign -dv` reports `Signature=adhoc`, `TeamIdentifier=not set`, and `codesign --verify --deep --strict` fails. Windows and Linux binaries were not built or executed. Production signing/artifact trust remains open; native notifications remain TASK-029. | `pnpm package:desktop`, runner `1790161989709411000.json`; `codesign -dv --verbose=2 apps/desktop/release/mac-arm64/YuanpuAgent.app` and `codesign --verify --deep --strict ...` from this worktree. |
| V21-08 | **Required command pass; aggregate acceptance unverified.** `pnpm check` passed build, Yuanpu typechecks and all test suites (Runtime 26/26). Evidence audit finds V21-01/02/04/05/06/07 still have full-journey or platform gaps, so no whole-card pass or completion claim follows from the green gate. | `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-pnpm-check --timeout 900 --require node --require pnpm -- pnpm check`; record `1790161868109883000.json`. |

Summary: V21-03 passed in isolated fixture mode; V21-01/05/06/07 have passed subchecks but remain unverified at their full acceptance boundary; V21-02/04 are unverified; V21-08's command passed but its evidence audit keeps the card open. No confirmed product defect was found. The first pre-build Runtime test failure and first renderer navigation failure are harness/setup observations, each followed by a passing run; they are not silently counted as product passes. Native notification belongs to 提醒界面与原生通知验收（TASK-029） and is not claimed here.

## Resume boundary

- Primary-agent operational observation, separate from the independent verifier's fixture claims: the formal App window initially ran a preload loaded before TASK-020 and showed `desktop.listSchedules is not a function`. After a normal quit/restart from current `main`, the live schedule page rendered its empty Runtime-backed list and the connection page loaded without a missing bridge method. No product source change or credential operation was needed.
- Next isolated scenarios: V21-02 future-plan exit/restart after due time, V21-04 provider-send-in-flight exit/restart, and V21-05/06 packaged-App update/rollback using disposable data. Keep each case tied to the tested revision and do not treat existing lower-layer tests as the full journey.
- Real V21-01 needs coordination with the one authorized private-chat user for a fresh non-sensitive message and a bounded one-time schedule; do not reuse prior TASK-019 messages as current-revision proof. V21-07 Windows/Linux require actual target-platform runners or hosts. A valid macOS Developer ID identity is not available; retain the production-signing deficit rather than weakening verification.
- TASK-021 remains `in_progress`; the ignored report must be force-added on this task branch for durable handoff. No completed-card claim or integration is authorized by these partial results.

## Continued isolated verification on `3a2fa1b` (2026-09-23)

Source revision: `3a2fa1b13d5fa7a87285ce014227d38c1b6c3780` plus only the three TASK-021 probes and this report. Environment: macOS arm64, Node 24.15.0, pnpm 11.22.0, synthetic input, ephemeral `YUANPU_HOME`, Electron userData, SQLite and controlled provider. No personal App process, live robot, Keychain credential or personal database was accessed. Retrieval: ZG semantic query for schedule/IM restart relationships returned fresh snippets in `docs/agent-runtime-contracts.md`; focused `rg` and reads of scheduler/channel tests and services identified the executable seams. Existing TASK-019/020 evidence remained historical and was not substituted for this revision's runs.

| Scenario | Expected and observed | Result, command and private runner record |
| --- | --- | --- |
| V21-02 plan waiting at App quit | An isolated production Electron main/preload/Runtime created a one-time schedule due five seconds later through trusted IPC. The App quit before the due time; its Runtime child exited and the controlled model saw zero requests both immediately and after the due time while the App was down. Reopening the same isolated home produced one `submitted` trigger with one `succeeded` run; IPC history/list and read-only SQLite agreed on one trigger, one run and one model request. Second quit removed Runtime again. This proves the bounded coalesce-on-restart path, not every misfire policy or full renderer navigation. | **Pass (isolated App fixture).** `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-waiting-schedule-app-final --timeout 180 --require node -- node apps/desktop/test/task-021-waiting-schedule-app-probe.mjs`; `1790162590378760000.json`. Earlier runs `1790162423462606000.json` and `1790162572609498000.json` reached the one-run state but failed harness assertions: the persisted trigger state is `submitted` (not a nonexistent `completed` state), and `node:sqlite` returns null-prototype row objects. Corrected without changing the business assertions. |
| V21-04 IM reply in flight | Real-file SQLite and a controlled transport held the reply after its outbound entered `delivering`. Channel shutdown changed it to `unknown`. A new router/AgentService over the same SQLite received the same inbound message as duplicate, returned the original run ID, and neither executed Agent again nor sent another reply (`executions=1`, `sends=1`). This proves channel lifecycle/reopen behavior; the provider transport is synthetic and an Electron App process was not shut down at that exact send boundary. | **Pass at channel persistence boundary; full App/provider journey still unverified.** `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-channel-inflight-restart --timeout 120 --require node -- node --test packages/yuanpu-runtime/test/task-021-channel-restart.test.mjs`; `1790162645434653000.json`. |
| V21-05 packaged SEA activation | The actual SEA copied from the freshly packaged macOS `.app` started through `RuntimeManager(packaged=true)` with disposable resources/userData and created a future schedule. After stopping, the same genuine SEA was staged with an exact SHA-256, activated on the next manager start, passed Runtime health and the stability interval, and preserved the schedule ID and SQLite row. This manually constructed the staging metadata; it did not exercise a remote manifest/download, a newer binary version, or a packaged Electron UI. | **Pass at packaged Runtime/SEA boundary; full App update journey still unverified.** `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-packaged-sea-update-rollback --timeout 180 --require node -- node apps/desktop/test/task-021-packaged-runtime-update-probe.mjs`; `1790162747927994000.json`. Stage/download checks were separately passed on `31fe1a3` as recorded above. |
| V21-06 packaged activation failure | After the confirmed activation, a controlled executable passed the version smoke (`9.9.9`) but exited before Runtime readiness. The packaged-mode manager restored the prior genuine SEA pointer, reopened the same schedule, and emitted the explicit restored-version error; SQLite retained one schedule. The earlier updater test checked database snapshot restoration after a simulated provisional migration. This probe does not verify an incompatible-protocol message rendered in the packaged App or a real version-changing migration. | **Pass at packaged Runtime rollback boundary; full UI/data-migration claim still unverified.** Same command/record `1790162747927994000.json`. |

Primary-agent observation, kept separate from this independent fixture work: at main `31fe1a3`, the formal App UI showed one authenticated private connection with one paired member. Read-only non-content SQLite counts for inbound/outbound/IM runs changed from 5/5/5 to 6/6/6 after a newly coordinated user message; the latest run was `succeeded`, outbound `accepted`, and the user confirmed seeing a reply. The primary agent owns that live evidence and any later scheduled-message coordination. It is useful partial V21-01 evidence, but this verifier did not perform it and it does not yet establish the complete same-revision App schedule/result navigation journey.

Current aggregate: V21-02 and V21-03 pass in isolated App fixture mode; V21-04 passes at channel persistence boundary while its full Electron/provider combination remains open; V21-05/06 now pass at the packaged Runtime boundary while packaged UI and genuinely newer-release migration remain open. V21-01 and Windows/Linux V21-07 remain outside this isolated run. These limits retain TASK-021 as `in_progress`; no product defect is established by the bounded probes.

After adding the three probes, `pnpm check` passed on this same source revision plus the TASK-021-only diff with Node 24.15.0/pnpm 11.22.0 (runner `1790162793285388000.json`, Runtime 26/26; the new channel test also passed in the runtime-kit suite). Reproduce via `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-continued-pnpm-check --timeout 900 --require node --require pnpm -- pnpm check`. The command again added only pnpm's empty lockfile checksum; that generated diff was removed. No product source or Pi upstream file was edited.

## V21-01 live one-user journey: independent read-only audit

- Execution owner and user observation: the primary agent operated the formal App at integrated `main` `31fe1a3859821366bf12b3cc83dd74b1fcad4dac` on macOS arm64. The preconfigured connection appeared authenticated with one paired private member. Following a fresh coordinated private message, the user explicitly confirmed seeing the reply. The primary agent created a one-time channel schedule for 2026-09-23 19:48 Asia/Shanghai, observed one successful run and one delivered IM attempt, and navigated in the formal App from task detail to the run conversation and back. Separately from the SDK/database receipt, the user answered the explicit scheduled-delivery visibility question “看到了” (“saw it”). A user screenshot was observed by the primary agent but is not copied, read or tracked by this verifier. The independent audit branch/source baseline was `3a2fa1b13d5fa7a87285ce014227d38c1b6c3780` plus the TASK-021-only probe/report diff.
- Independent verifier method: three read-only aggregate/boolean `sqlite3 -readonly -json` queries against the formal App workflow SQLite. The queries selected only counts, booleans and status predicates; no raw identifiers, digests, messages, prompts, Secret, Bot ID or environment file were selected or printed. They did not mutate the running App, connection or database.
- IM persisted facts: `action='run'` inbound count = 6, outbound count = 6, IM Agent run count = 6, matching the primary agent's before/after counts of 5/5/5 → 6/6/6. The latest `action='run'` inbound joined exactly one `single`/`text` row to its own `im` Agent run (`succeeded`) and same-run outbound (`accepted`). This proves local persisted relationships and SDK acceptance, while the user's confirmation supplies the separate client-visibility observation.
- Schedule persisted facts: one schedule, one trigger and one delivery-attempt record. The trigger's `scheduled_at` was in the expected `2026-09-23T11:48` UTC minute (19:48 Asia/Shanghai), with matching schedule revision and a linked `scheduler` run `succeeded`; delivery was `delivered` with `attempts=1`. The schedule's delivery kind was `channel`, and its one-time `next_trigger_at` was null after execution. These facts support one recorded execution/attempt without duplicate local records; the user's confirmation supplies actual scheduled-message visibility. App task-detail status and conversation navigation are primary-agent UI observations, not independently exercised in this audit.
- V21-01 verdict: **Pass for the bounded live path through an existing configured, paired, single-user private connection, fresh IM exchange, one-time channel schedule, result inspection/navigation and user-confirmed delivery, with independent read-only persistence audit.** Fresh credential provisioning or connection reconfiguration was not exercised, nor were multiple users/chats, offline provider recovery or a packaged App installation. This result does not complete V21-05/06 packaged UI/update or V21-07 Windows/Linux and production signing. Native notification remains outside TASK-021 under TASK-029. TASK-021 remains `in_progress`.

The user subsequently confirmed again that the 19:48 scheduled message was visible in the Enterprise WeChat private chat. This is a second user observer confirmation, separate from the database/SDK delivery state; neither the screenshot nor message body is retained here.

Reproduce the sanitized persistence audit (read-only, aggregate results only):

```sh
sqlite3 -readonly -json /Users/linxinhong/.yuanpu/workflows/automation.sqlite "SELECT (SELECT COUNT(*) FROM yp_channel_inbound WHERE action='run') AS im_inbound_runs, (SELECT COUNT(*) FROM yp_channel_outbound) AS im_outbounds, (SELECT COUNT(*) FROM yp_agent_runs WHERE entry_point='im') AS im_agent_runs, (SELECT COUNT(*) FROM yp_schedules) AS schedules, (SELECT COUNT(*) FROM yp_schedule_triggers) AS schedule_triggers, (SELECT COUNT(*) FROM yp_delivery_attempts) AS delivery_attempt_records;"
sqlite3 -readonly -json /Users/linxinhong/.yuanpu/workflows/automation.sqlite "WITH latest_im AS (SELECT inbound_id, run_id, conversation_type, message_type, action FROM yp_channel_inbound WHERE action='run' ORDER BY received_at DESC LIMIT 1) SELECT COUNT(*) AS rows, SUM(CASE WHEN i.conversation_type='single' AND i.message_type='text' AND i.action='run' THEN 1 ELSE 0 END) AS private_text_runs, SUM(CASE WHEN r.entry_point='im' AND r.status='succeeded' THEN 1 ELSE 0 END) AS succeeded_runs, SUM(CASE WHEN o.status='accepted' AND o.run_id=i.run_id THEN 1 ELSE 0 END) AS accepted_linked_outbounds FROM latest_im i JOIN yp_agent_runs r ON r.run_id=i.run_id JOIN yp_channel_outbound o ON o.inbound_id=i.inbound_id;"
sqlite3 -readonly -json /Users/linxinhong/.yuanpu/workflows/automation.sqlite "SELECT COUNT(*) AS schedules, SUM(CASE WHEN json_extract(s.delivery_json,'$.kind')='channel' THEN 1 ELSE 0 END) AS channel_schedules, SUM(CASE WHEN s.next_trigger_at IS NULL THEN 1 ELSE 0 END) AS one_time_completed, SUM(CASE WHEN t.schedule_revision=s.revision THEN 1 ELSE 0 END) AS matching_revision, SUM(CASE WHEN substr(t.scheduled_at,1,16)='2026-09-23T11:48' AND t.status='submitted' AND r.entry_point='scheduler' AND r.status='succeeded' THEN 1 ELSE 0 END) AS succeeded_expected_trigger, SUM(CASE WHEN d.status='delivered' AND d.attempts=1 THEN 1 ELSE 0 END) AS single_delivered_attempts FROM yp_schedules s JOIN yp_schedule_triggers t ON t.schedule_id=s.schedule_id JOIN yp_agent_runs r ON r.run_id=t.run_id JOIN yp_delivery_attempts d ON d.run_id=r.run_id;"
```

## Packaged macOS Electron App, isolated on `f8d5d01` (2026-09-23)

- Tested branch revision `f8d5d01a207bd44793ebd8d93d759652435b1b77` plus only TASK-021 probe/report changes. macOS arm64, Node 24.15.0, pnpm 11.22.0. The actual freshly built unpacked `.app` under this worktree's `apps/desktop/release/mac-arm64/` was launched; no installed or running personal App bundle was used. Its resources stayed in the worktree. Synthetic config, `YUANPU_HOME`, Chromium userData, update staging and SQLite were under one temporary directory removed by the probe. Native notifications were disabled. No real channel connection, credential, user or external model request was configured.
- Isolation preflight: Electron 43's CLI `--user-data-dir` made `app.getPath('userData')` resolve to the disposable directory (macOS canonicalized `/var` to `/private/var`). The first preflight assertion compared those two path spellings and failed only for that alias; canonical-path comparison passed. `task-021-packaged-path-preflight.mjs`, runner `1790164763377291000.json`. The packaged probe then passed the same flag and set only `YUANPU_HOME` to a disposable home. It did not repurpose the shell's HOME.
- V21-05 **pass for packaged-App activation with bounded synthetic data**: actual packaged Electron main/preload/renderer authenticated to the bundled SEA. A synthetic future schedule created through preload appeared in the packaged UI. Browser-protocol `Browser.close` quit the App and removed its Runtime child. A byte-identical genuine SEA from the same package was manually staged with SHA-256 in disposable userData; on the second packaged App launch, it activated, passed health and stability confirmation, and the same schedule remained visible through the packaged UI and present once in SQLite. This establishes App/SEA activation and schedule persistence, but not a newer-version binary migration, remote manifest download or Pi conversation persistence. Initial two-launch probe runner `1790164887832324000.json`; final three-launch probe runner `1790164966651888000.json`.
- V21-06 **rollback pass, explicit user-facing incompatibility notice fail (D21-01)**: one controlled staged candidate returned version `9.9.9` to the updater's smoke check, then emitted a readiness event with protocol `999`. On the third actual packaged App launch, the host identified `Runtime protocol is incompatible`, rolled back to the previously confirmed genuine SEA, and the schedule remained available. The diagnostic appeared on the host stream, but no compatibility notice appeared in the renderer (`visibleIncompatibilityNotice=false`); the recovered start did not enter `main.ts`'s fatal-start `dialog.showErrorBox` path. The observed recovery path calls `RuntimeManager.reportError`, whose default is `console.error` and has no renderer event. This fails the card's clear user-facing incompatibility prompt for a recovered incompatible update. A native OS dialog was not separately instrumented, but the exercised source path has no dialog invocation. Defect D21-01 is scoped to the update-failure presentation; rollback/data preservation passed. Reproduce with `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root <worktree> run --label task-021-packaged-electron-incompatible --timeout 150 --require node -- node apps/desktop/test/task-021-packaged-electron-app-probe.mjs` (runner `1790164966651888000.json`). Do not mark this scenario or TASK-021 fully passed until a repair is integrated and this probe is rerun.
- V21-07 remains unverified for Windows/Linux and production signing. This package is ad hoc signed, as recorded above; the isolated executable probe does not establish distributable trust, notarization or native notification behavior. No personal App, bot, data or settings were modified.

## D21-01 repair review and independent packaged retest (TASK-030)

- Repair source: `task/task-030-runtime-recovery-notice` commit `758f808cdaaa898ce55fd6a8461584d52d72d13f`; the implementer reports Node 24 `pnpm check` passing, and the repaired macOS arm64 `.app` was produced by `pnpm package:desktop` (runner `1790165725867983000.json`). Independent probe source: TASK-021 branch `021375b5b20eeecc683af70414e3e67511c49851` plus the TASK-021-only probe/report diff. This section describes a repair-branch test, not integrated-main acceptance.
- Read-only design review: `RuntimeManager` classifies a mismatch in either the ready line or authenticated health response as `incompatible_protocol`; only after a provisional update fails and the previous Runtime successfully relaunches does it invoke `onUpdateRecovery`. All other recovered launch failures map to `activation_failed`. Update pointer/SQLite rollback code was not changed. Desktop main stores only the fixed `{kind}` enum and serves it through `trustedHandler` IPC to the preload getter; the renderer displays fixed copy and no raw stderr, filesystem path, provider identity or credentials. Main completes `runtime.start()` before creating the window, so the initial renderer fetch can observe the recovery reason. Normal starts leave the notice undefined. The close action clears renderer state; a renderer reload in the same process may show the stored notice again, a bounded UX caveat rather than a failure of the tested close action. No TASK-030-specific automated test was added in the repair commit, so the packaged retest below is the independent behavior evidence.
- Probe change: `apps/desktop/test/task-021-packaged-electron-app-probe.mjs` accepts `TASK_021_PACKAGED_APP_PATH`, with its original bundle path as default. In each isolated launch it uses temporary `YUANPU_HOME`, Chromium userData and SQLite. First normal launch and second confirmed byte-identical SEA activation both return no recovery notice from the trusted preload and show no notice after renderer navigation mounts. A staged synthetic version `9.9.9` candidate emits protocol `999`; the third launch rejects it, restores the genuine SEA pointer, keeps the original schedule visible in packaged UI and SQLite, and returns only `kind=incompatible_protocol` through the preload getter. The alert contains the fixed incompatible/recovered wording, has a nonzero layout box and visible computed style, contains neither `999` nor the temporary path, and disappears after its Close button is clicked. A fourth launch stages a candidate which passes `--version` with `9.9.10`, then emits the non-sensitive marker `TASK021_SYNTHETIC_DIAGNOSTIC_MARKER` to stderr and exits before readiness. The host diagnostic proves the failure fired; rollback restores the genuine SEA and original schedule. The trusted getter returns only `kind=activation_failed`, and the visible, dismissible fixed-copy alert contains neither the marker nor the temporary path. All four App exits removed their Runtime children. No real bot, user conversation, model provider, credential, notification, installed App or personal data was used.
- Result: **D21-01 passes independently on the repair branch's packaged App** for normal no-false-positive, protocol-incompatibility and generic-activation-failure presentation, dismissal, rollback and schedule retention. Reproduce: `TASK_021_PACKAGED_APP_PATH=/Users/linxinhong/projects/yuanpu-agent/.worktrees/codex-task-030/apps/desktop/release/mac-arm64/YuanpuAgent.app python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root /Users/linxinhong/projects/yuanpu-agent/.worktrees/codex-task-019 run --label task-021-task030-generic-sanitized --timeout 180 --require node -- node apps/desktop/test/task-021-packaged-electron-app-probe.mjs`; runner `1790165972139815000.json` exits 0 with `packagedAppLaunches=4`, `visibleIncompatibilityNotice=true`, `genericFailureSanitized=true`, `persistedScheduleVisible=true`, `incompatibleCandidateRolledBack=true`, `runtimeChildrenStopped=true`. Earlier three-launch runner `1790165867257472000.json` passed before the generic case was added. Product package source is `758f808`, which is now incorporated by main merge `90e5787`; this result still tests that repair-worktree-built package, not a rebuilt current TASK-021 revision. TASK-021 remains `in_progress`; full cross-platform and signing evidence remains unavailable.

## Current integrated-revision focused retest

- TASK-021 verification branch merged `main` `90e57876c50d9e911d1117741939d856fb45b04a` as `ed13cdef0c714ac45ffa972936c8b6d5dfd98c1b`. This preserves all TASK-021 evidence commits and incorporates TASK-030's product repair. The merge was clean; no product or Pi upstream source was edited by this verifier. Host: macOS arm64, Node 24.15.0, pnpm 11.22.0.
- `pnpm package:desktop` rebuilt the actual macOS Electron `.app` and archive from `ed13cde`, runner `1790166123605035000.json`, exit 0. The bundle remains ad hoc/untrusted for production: packaging explicitly skipped Developer ID signing. `pnpm`'s generated empty lockfile checksum was removed; no tracked lockfile delta remains.
- The four-launch isolated packaged App probe ran against that rebuilt TASK-021 bundle, not the older repair-worktree package: `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root /Users/linxinhong/projects/yuanpu-agent/.worktrees/codex-task-019 run --label task-021-integrated-packaged-recovery --timeout 180 --require node -- node apps/desktop/test/task-021-packaged-electron-app-probe.mjs`; runner `1790166220699941000.json`, exit 0, source `ed13cde`, `changed_during_run=false`. Result: `packagedAppLaunches=4`, `persistedScheduleVisible=true`, `stagedSeaConfirmed=true`, `incompatibleCandidateRolledBack=true`, `visibleIncompatibilityNotice=true`, `genericFailureSanitized=true`, `runtimeChildrenStopped=true`. Normal launches had no recovery false positive; protocol and general activation failures each restored the old SEA and schedule; both fixed-copy alerts were visible, dismissible and free of the synthetic marker/temporary path.
- Focused `node --test apps/desktop/test/runtime-manager.test.mjs apps/desktop/test/runtime-updater.test.mjs` passed on `ed13cde` (runner `1790166258386575000.json`). No full `pnpm check` was rerun after the merge; the repair owner had passed it on `758f808`, and TASK-021 had passed it before integration. The current-revision claim is therefore bounded to successful packaging, this packaged behavior probe and focused manager/updater tests, not a new full-workspace gate.
- D21-01's user-visible recovered-update defect is **verified fixed on this integrated-revision macOS package**. TASK-021 remains `in_progress`: genuine newer release / migration / remote download, Windows/Linux executable paths, production signing, and broader real multi-user/provider outcomes are not established by these isolated checks. Native notifications remain TASK-029.

## V21-04 App exit during unresolved IM reply (continued isolated check)

- Baseline source `197f7138a26a3a325b35bdf756cfa8b4b8fd1f1e` plus only `apps/desktop/test/task-021-im-{app-restart-probe,runtime-fixture}.mjs` and this report; macOS arm64, Node 24.15.0, pnpm 11.22.0. The expected facts come from the TASK-021 card and `docs/agent-runtime-contracts.md` / `docs/im-channel-contract.md`: after uncertain provider receipt, IM outbound is `unknown`, Agent is not rerun, reply is not blindly resent, and execution versus delivery must be distinguishable. Retrieval was bounded exact `rg` across those contracts, Runtime channel assembly, Electron main/preload, and App run detail; no ZG tool was available on this host.
- Fixture boundary: production Electron main/preload and built React renderer launched twice with disposable `YUANPU_HOME` and userData. The child was a test-only Runtime HTTP/bootstrap process using the **real** `ChannelRouter`, `PersistentAgentService`, and SQLite stores from `@yuanpu-agent/runtime-kit`, but substituting a synthetic inbound and controlled transport/Agent executor. It never called system Keychain, WeCom SDK, a model provider or any real IM endpoint. This is deliberately **not** a claim that unmodified production Runtime/provider ran the scenario; that assembly offers no injectable credential/transport seam without touching real Keychain or altering product code.
- Observation: one synthetic inbound completed its Agent run while the controlled reply remained unresolved at persisted `delivering`. Electron App quit normally and its child stopped. Relaunching the same isolated home changed that single outbound to `unknown`; replaying the same inbound returned the original run as a duplicate. Read-only SQLite reported one inbound, one outbound, one IM run (`succeeded`) and outbound `unknown`; fixture counters stayed at one Agent execution and one send. Both App quits left no child Runtime. **Pass for the App-process / real channel-persistence fixture boundary**, extending the prior channel-only test, but the full formal Runtime/WeCom journey remains unverified.
- UI defect candidate D21-02: after restart the trusted `getAgentRun(runId)` bridge returned the IM run as `succeeded` with no outbound/delivery status. The mounted renderer had no `outbound`/IM-delivery query bridge and did not display `投递结果未知`; the App run-detail component renders only `locatedRun.status` for an IM run, while the separate delivery label exists only for schedule history. The probe does not claim that it navigated to an IM run card through a real notification, but the public bridge and rendered component currently have no persisted outbound status to present even when the run is located. This violates the expected App distinction between Agent success and uncertain IM delivery; route to an implementation owner rather than changing product in TASK-021. Do not mark the whole V21-04 acceptance passed.
- Reproduce: `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root /Users/linxinhong/projects/yuanpu-agent/.worktrees/codex-task-019 run --label task-021-im-app-restart-final --timeout 150 --require node -- node apps/desktop/test/task-021-im-app-restart-probe.mjs`; runner `1790171154054845000.json`, exit 0, `changed_during_run=false`, summary `appLaunches=2`, `outbound=unknown`, `imRuns=1`, `sends=1`, `agentExecutions=1`, `rendererMounted=true`, `imDeliveryVisible=false`, `outboundBridge=false`, `runtimeChildrenStopped=true`. An initial probe run `1790171124417628000.json` also passed before the explicit bridge/status-field assertions were added.
- Required `pnpm check` passed on the same product revision plus the TASK-021-only probe/report diff (runner `1790171206993229000.json`, Node 24.15.0/pnpm 11.22.0). It added pnpm's unrelated empty lockfile checksum during execution; that generated change was removed afterward. The new probe is a standalone targeted executable rather than part of the default suite, so its explicit runner above is the behavioral evidence.
- A genuinely newer SEA activation/migration or remote manifest-download journey was not attempted in this continuation. The existing staged same-version `0.1.0` SEA remains same-version evidence only. Producing a newer release artifact without touching product source or release provenance, plus representative migration data and trust, requires separate bounded setup; no such claim is made here.

## D21-02 independent repair review and V21-04 retest (TASK-033)

- Repair sources: `06312e90c45e8abfcac86a28d20d85ed756ba766`, then authorization-link hardening `c7bfe956049a87612ec87a100634693a5b679899` and `193fe5a` on `task/task-033-im-delivery-status`. After a clean-worktree preflight, TASK-021 merged the branch in `2529ec8115b8cd3769fe4b05a9c275f3fd04f813`, committed its test-only probe adaptation as `1430743`, and merged the last hardening as `6ceed8fb38fd95c2630d05ecfb7ad5d2f43c9774`. The task branch still owns this report and probe; TASK-033 product source was only merged, not edited by the verifier. Host: macOS arm64, Node 24.15.0, pnpm 11.22.0.
- Read-only review of the source and card: the new Runtime route returns only `{runId, runStatus, replyDeliveryStatus}`, not message text, URL, sender ID/digest, bot ID, credential reference or failure details. The Runtime checks a real `im`/`channel_user`/`channel_adapter` run, same workspace and connection, `single` inbound action, current paired sender digest, run conversation ID `single:<inbound.conversationDigest>`, run delivery route equal to inbound ID, and outbound run/inbound IDs equal to the authorized records. The last outbound-link check arose from this verifier's independent review; it prevents a valid run's summary from reflecting an unrelated outbound row even if SQLite contains cross-linked data. Missing/nonpaired/foreign relationships return 404. The preload/main IPC is a narrow, trusted-renderer-only getter; `accepted` is labelled SDK acceptance, not human visibility. The App presents Agent status and reply delivery in separate rows, and has a sanitized IM fallback when generic desktop run access denies the IM record. This is static review plus the targeted behavior below, not an assertion that every malformed database shape or OS was exercised.
- Updated V21-04 probe reuses the prior controlled IM send held at `delivering`, Electron quit, and same-home restart. Its test-only Runtime now imports the actual repaired summary-authorization helper, serves the new route, and makes generic desktop `getAgentRun` return 404 for IM as production does. Through the actual repaired main/preload bridge, the probe obtains an exact three-field `succeeded`/`unknown` summary, then removes the paired sender from the isolated connection document and observes a denied query; restoration allows it again. A test-only Electron wrapper emits the production `notifications:navigate` event after the renderer mounts. The real React renderer then shows `运行状态 succeeded` and separately `回复投递 投递结果未知（不会自动重发）`; after reload and re-navigation the same persisted status remains. The visible run card and returned summary contain no synthetic sender, reply body or URL. SQLite still has one inbound/outbound/IM run, Agent executions and transport sends remain one each, and both Runtime fixture children stop with the App. The fixture avoids Keychain/WeCom/model access; it does not run the unmodified production Runtime route or real provider transport, and the navigation event is injected by a test-only wrapper rather than a real OS notification. Native notification behavior belongs to TASK-029.
- Evidence: final adapted probe `python3 ~/.agents/skills/coding-owner/scripts/worktree-kit.py --root /Users/linxinhong/projects/yuanpu-agent/.worktrees/codex-task-019 run --label task-021-task033-final-sanitized-ui --timeout 150 --require node -- node apps/desktop/test/task-021-im-app-restart-probe.mjs`, runner `1790172599019589000.json`, exit 0, head `6ceed8f`, `changed_during_run=false`, summary `appLaunches=2`, `outbound=unknown`, `imRuns=1`, `sends=1`, `agentExecutions=1`, `imDeliveryVisible=true`, `refreshRetained=true`, `pairedOnly=true`, `summarySanitized=true`, `runtimeChildrenStopped=true`. Same scenario passed before the final visible-card sanitization assertions in runners `1790172405358985000.json` (before last hardening) and `1790172518943829000.json` (after). Focused `node --test apps/runtime/test/runtime-host.test.mjs` passed at `6ceed8f` (runner `1790172512528318000.json`). Required `pnpm check` passed on that same product revision and test-only probe state (runner `1790172530489632000.json`); pnpm's unrelated empty lockfile checksum was removed after execution.
- Verdict: **D21-02's separated, paired-only, sanitized IM delivery presentation passes the bounded TASK-021 fixture App retest on the repair branch.** It closes the observed fixture UI failure from `905c887`; V21-04's formal Runtime/real WeCom in-flight exit/restart remains unverified, and TASK-021 remains `in_progress`. The other delivery labels (`not_created`, `pending`, `delivering`, `accepted`, `failed`) have source/owner test coverage but were not each exercised in this two-launch App probe. TASK-033 is not represented as integrated main or completed by this report.

## Current-main resumption on `9055da0` (2026-09-24)

- Ownership: resumed the existing clean `task/task-021-business-acceptance` worktree; it fast-forwarded from `1de5775` to local main `9055da0` without incorporating main's uncommitted/untracked user files. This revision includes TASK-033/034 and the TASK-036 chat UI. Host: macOS arm64, Node 24.15.0 / pnpm 11.22.0 through the saved coding-owner toolchain. Retrieval reused this report, the exact TASK-021 card, TASK-020/030/033/034 memories and focused `rg` in the existing probes/Runtime contracts; no new semantic workspace discovery was needed.
- `pnpm check` passed before modifying the TASK-021-only probe (`1790221090939574000.json`). `pnpm package:desktop` passed from the same product revision, including native SEA/Python and update smoke (`1790221187331576000.json`). The unmodified four-launch packaged macOS App probe passed (`1790221295258653000.json`).
- Expanded only `apps/desktop/test/task-021-packaged-electron-app-probe.mjs` to use a loopback synthetic model and create one successful desktop run/binding plus one disabled synthetic connection before staging the byte-identical genuine SEA. On the second packaged launch the same run was retrievable, the connection and schedule remained listed, SQLite still had exactly one desktop binding/run/schedule, and provider request count stayed one. The two failed-candidate launches still preserved that run and schedule while showing dismissible sanitized recovery notices. Four-launch probe **passed** (`1790221369294762000.json`): run and binding were retained, `disabledConnectionRetained=true`, `providerRequests=1`, `runtimeChildrenStopped=true`. This extends V21-05/06's same-version isolated persistence evidence; it does **not** prove Pi conversation content retention, newer-version migration, remote manifest/download, authenticated IM binding, or production-signing.
- Current-revision focused regressions passed: V21-04 two-launch controlled IM-delivery App/SQLite fixture (`1790221512530261000.json`), V21-02 waiting-plan App restart (`1790221530114591000.json`), and V21-03 active-run App restart (`1790221545241181000.json`). The V21-04 Runtime/provider remains test-only, not formal WeCom. No personal data, credential or real IM message was used in these probes.
- The first post-probe `pnpm check` (`1790221562227937000.json`) failed one desktop `runtime-manager` crash/restart test with `fetch failed`/`ECONNRESET` while other reported cases passed. The exact test then passed in isolation (`1790221682624617000.json`), and the unchanged full tree passed `pnpm check` on retry (`1790221706385077000.json`). This is an observed non-stable test failure, not silently counted as a first-run pass; there is no reproducible product defect from these runs. Both broad commands generated only pnpm's unrelated empty lockfile checksum, removed afterward.
- V21-01 current-revision live IM still awaits a coordinated normal App restart and fresh authorized private message; the 2026-09-23 live path remains valid only for its earlier tested revision. A pre-existing App/Runtime process was found on the Mac, so no second live connection was opened. The user said they are available for a fresh message and was asked to quit the older App normally before this verifier starts the current build. V21-07 Windows cloud desktop was awakened, but no current-revision packaged App journey was executed there; Ubuntu likewise remains untested on current revision. TASK-034 CI is build/smoke evidence only. Production signing/trust remains open, and native notifications remain TASK-029. TASK-021 is still `in_progress`.
- Independent read-only evidence audit after `6e15bcd` found the packaged probe's `desktopConversationRetained` summary broader than its binding/run-status assertions. The summary was narrowed to `desktopRunAndBindingRetained`, and the four-launch probe passed again on the same packaged binary (`1790222444118745000.json`). The audit confirms V21-01's prior bounded real result, V21-02/03/04's fixture boundaries and the macOS same-version packaged recovery, but not Pi conversation content, a newer SEA migration, formal Runtime/WeCom in-flight recovery or Windows/Linux App journeys. No product source was changed by this correction.
- Current-revision live readiness attempt: after the user normally quit the older main App, process checks showed its Electron and Runtime children gone. `pnpm dev` from this TASK-021 worktree started exactly one new Electron/Runtime pair, and the App showed local Runtime connected plus one WeCom connection `已连接` with one paired private member. No Bot ID, Secret, member ID or message body was read. The read-only SQLite inbound/outbound/IM-run baseline was 6/6/6; repeated aggregate checks during the 20-minute listening window stayed 6/6/6. The user had said a test was convenient, but had not confirmed sending one during this window, so **no current-revision live message/reply pass or product failure is claimed**. The bounded `pnpm dev` runner timed out as configured (`1790221997238116000.json`, exit 124) and stopped its Electron/Runtime children; a follow-up process check found none remaining. A fresh live test requires starting a single App instance again before asking the user to send.

## Current-revision real one-user regression (2026-09-24)

- Tested tree: TASK-021 branch `ae83e44` over product baseline local main `9055da0`, macOS arm64, Node 24.15.0 / pnpm 11.22.0. The App was started as a single `pnpm dev` Electron/Runtime pair with the coding-owner bounded runner `task-021-live-retry-current-app` (3600-second cap); UI showed local Runtime connected and the existing Enterprise WeChat connection connected with exactly one paired private member. The pre-existing pairing was used; this did not test fresh credential provisioning or a second member. The prior no-reply report occurred around an earlier bounded listener's expiry, and its exact send time cannot be established; it is neither counted as a confirmed product defect nor erased by this retry.
- V21-01, new private reply: before the newly coordinated message, read-only personal workflow SQLite aggregate counts were inbound/outbound/IM runs `6/6/6`; afterwards they were `7/7/7`. A content-free join of the latest inbound, Agent run and outbound returned `linked_rows=1`, `private_text=1`, `agent_succeeded=1`, `reply_accepted=1`; every recorded outbound was `accepted` and none `unknown`. Separately from SDK acceptance, the user reported **“已收到”** for the reply. No credential, sender ID, message body or response URL was read or copied.
- V21-01, one-time scheduled private delivery: in the same running App, created one harmless one-time plan for `2026-09-24 13:30 Asia/Shanghai` targeting the already paired private chat. The task preview and saved list showed the same due time. After the due time, aggregate scheduler counts changed from triggers/deliveries/scheduler runs `1/1/1` to `2/2/2`. A content-free join scoped to this uniquely named test plan found exactly one `submitted` trigger, one `succeeded` Agent run and one `delivered` delivery attempt (`attempts=1`). The one-time plan was automatically disabled (`enabled=0`) with no next trigger. The App's refreshed task detail separately displayed Agent execution success and message delivery `已送达`; “查看会话” opened its run/conversation, and “返回定时任务” returned to the same task detail and execution record. Separately from persisted delivery status, the user answered **“看到了”** for this new scheduled message in the real WeCom private chat.
- Bounded verdict: **V21-01 passes for the already configured, single-authorized-member private IM → scheduled delivery → task/run navigation path on this current product revision and macOS dev App.** Fresh configuration/rebinding, a packaged App using the real bot, and other users are not claimed. V21-02/03 and V21-04 isolated fixture conclusions, V21-05/06 same-version packaged SEA recovery, and V21-07 platform/signing limits remain as documented above. A genuinely newer SEA migration/remote manifest journey, formal Runtime/WeCom send interrupted exactly at App exit, Windows/Linux executable App journeys, and production signing/trust are still unverified. The notification UI/system delivery is TASK-029, not this card. Consequently TASK-021 remains `in_progress`; no full-card completion or publishability claim follows from this successful live path.
