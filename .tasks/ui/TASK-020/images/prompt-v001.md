# TASK-020 design-v001 prompt

- Source screenshot: `before-v001.png`, browser preview `http://127.0.0.1:5173/`, 1440×900 CSS and image pixels, DPR 1, scroll (0,0), synthetic greeting, no Electron bridge or personal data.
- Edit target: the sidebar navigation rows below `技能` and the entire inner content of the large right-hand chat panel. Keep the outer shell, sidebar brand/footer, dark green palette, panel bounds, typography character and compact spacing recognizable.
- Mode: GPT image edit of the actual screenshot, `ui-mockup`; target ratio 16:10 (1440×900). If the tool's output dimensions differ, do not stretch the reference; report the drift.

Prompt: Edit this existing Yuanpu Agent desktop screenshot into a high-fidelity management UI concept for the same application. Keep the 280 px sidebar, brand mark, footer, outer rounded main panel, dark charcoal/forest-green colors, restrained green accent and typography style substantially unchanged. Add two peer navigation rows under 技能: 连接 and 定时任务, with 定时任务 selected. Replace only the right panel's chat content with a dense but calm 定时任务 management view. Top bar: title 定时任务, concise Runtime connected indicator, one primary 新建任务 button. In the body, left portion is a compact list of three synthetic schedules (每日工作摘要, 每周回顾, 资料整理) showing enabled/paused/failed states, next trigger with time zone, and last run outcome. Right portion is the selected task detail: name, instruction summary, workspace, explicit output target 企业微信私聊, next scheduled time, edit/pause actions, and a readable run-history section that visually separates Agent run status from IM delivery status; one completed row and one waiting-approval row. Show a clear “查看会话” affordance. No raw IDs, Bot ID, secret, real user message, notification settings, gradients added beyond the existing shell, generic dashboard metric cards, or invented social/chat features. Use Chinese labels, synthetically plausible content, tidy alignment, visible focus/selection, restrained density and enough whitespace to scan. Treat exact generated text as illustrative; preserve the surrounding app shell.
