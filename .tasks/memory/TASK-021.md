# App 多入口与升级完整业务验收（TASK-021）—进行中

- 关键词：业务验收、企业微信私聊、定时任务、App 重启、SEA 升级、跨平台。
- 记录日期：2026-09-24；分支 `task/task-021-business-acceptance`；worktree `.worktrees/codex-task-019`。证据在 `.tasks/verification/TASK-021/results.md`；任务状态以 `.tasks/tasks.yaml` 为准。
- 最新产品基线：local main `9055da0`；TASK-021 探针与证据提交 `6e15bcd`、`9276ae8`。Node 24.15.0 / pnpm 11.22.0，macOS arm64；主工作区有他人未提交文件，未纳入任务分支。

## 已验证的边界

- 先前正式 App 在 `31fe1a3` 的单人企业微信私聊、一次性定时投递与结果导航有用户可见确认和只读 SQLite 关联审计；它不等于当前 revision 的真实路径通过，也没有重新配置新凭据。
- 当前 `9055da0` 的隔离 App 场景：等待中的计划、运行中的任务、投递中的 IM 回复分别退出重启，所测恢复结果通过；后者使用测试 Runtime/传输层，不是正式 Runtime＋企微的在途投递实验。
- 当前 macOS 打包 App 四次启动：同版本真实 SEA 激活，计划、桌面运行/绑定和停用连接保留；两种合成失败候选回退并显示脱敏提示，子进程清理。没有真正新版本迁移、Pi 会话正文恢复或远程清单下载证据。
- `pnpm package:desktop` 和 `pnpm check` 在 `9055da0` 加 TASK-021 探针上通过；第一次后探针全量检查曾有单个 `ECONNRESET`，聚焦复跑与完整复跑通过，详见报告。TASK-034 的三平台 CI 仅证明构建/原有 smoke，不证明 Windows/Linux App 用户旅程。
- 独立只读审查要求将探针输出由“会话保留”收窄为“运行与绑定保留”；已改并重跑通过。原生通知属于 TASK-029，生产签名/信任缺口不得当作已解决。

## 下次继续

- 当前版本单实例 App 已完成新的真实单人私聊及一次性定时任务复验：入站/出站/IM 运行计数 6/6/6 → 7/7/7，最新关联运行成功且回复被 SDK 接受，用户确认收到；13:30 的任务恰好一条触发/成功运行/已送达，用户确认看到；App 中任务详情→运行会话→返回任务详情正常。详见报告“Current-revision real one-user regression”。没有重新配置凭据、读取消息正文或扩展到第二用户。前一次“未收到”发生在旧监听到时前后，发送时间无法确认，不能判为产品缺陷。
- 真实新版本 SEA、迁移及远程下载需要可追溯的新版本候选与隔离代表性数据；没有候选时保留未验证，不能用同版本字节复制代替。Windows/Linux 需要各自包的可执行 App 旅程，当前云电脑可连接但尚未完成当前 revision 的包与执行证据。
- 后续完成前须在集成 main 上复核、更新本文件与验收报告，并通过任务工具执行 `preflight`/`complete`；目前不得标 done 或宣称可发布。
